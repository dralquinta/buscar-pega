interface:
  display_name: "Write Resume"
  short_description: "Tailor resumes while preserving layout"
  default_prompt: "Use $write-resume to tailor my resume to this job description while keeping the original CV format unchanged."
